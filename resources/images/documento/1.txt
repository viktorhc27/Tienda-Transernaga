SELECT pro_id,pro_stock,pro_precio_compra, pro_precio_compra * pro_stock as costo_total, (SELECT sum(ventas.ven_cantidad) FROM ventas WHERE ventas.productos_pro_id = 3 )as "unidades vendidas", pro_precio_venta as "costopor venta", pro_precio_venta* (SELECT sum(ventas.ven_cantidad) FROM ventas WHERE ventas.productos_pro_id = 3 ) as "ganancia", pro_stock +(SELECT sum(ventas.ven_cantidad) FROM ventas WHERE ventas.productos_pro_id = 3 ) as "unidad global" FROM productos WHERE pro_id = 3;